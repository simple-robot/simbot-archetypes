plugins {
    `simbot-archetypes-nexus-publish`
}

group = "love.forte.simbot.archetypes"
version = "0.0.1"

repositories {
    mavenLocal()
    mavenCentral()
}

allprojects {
    group = "love.forte.simbot.archetypes"
    version = "0.0.1"
    repositories {
        mavenLocal()
        mavenCentral()
    }
}
